let React = require("react");

let Client = React.createClass({

  render: function() {
    return (
      <div className="container">
        Hello friends
      </div>
    );
  }

});

module.exports = Client;
